import { ListasTodosLosTemas } from './listas-todos-los-temas';

describe('ListasTodosLosTemas', () => {
  it('should create an instance', () => {
    expect(new ListasTodosLosTemas()).toBeTruthy();
  });
});
